<div class="hero-box">
    <img src="{{ asset('/logo/loginLogo.png') }}" alt="Mindway Logo" class="hero-logo">
    <img src="{{ asset('/logo/logo.png') }}" alt="Mindway Logo" class="hero-logo-small">
    <h1 class="hero-title">{{ $title ?? 'Your All-in-One' }}</h1>
    <h1 class="hero-subtitle">{{ $subtitle ?? 'Platform for' }}</h1>
    <h1 class="hero-tagline">{{ $tagline ?? 'Employee Well-Being.' }}</h1>
</div>